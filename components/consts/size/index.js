export const SIZE = Object.freeze({
  Small: 'small',
  Medium: 'medium',
  Large: 'large',
});

export const SIZE_BOTTOM_SHEET = Object.freeze({
  Full: 'full',
  Half: 'half',
  Content: 'content',
});

export const SIZES = Object.values(SIZE);
export const SIZES_BOTTOM_SHEET = Object.values(SIZE_BOTTOM_SHEET);
